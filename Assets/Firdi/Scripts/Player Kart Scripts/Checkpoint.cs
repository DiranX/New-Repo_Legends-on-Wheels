using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Checkpoint : MonoBehaviour
{
    public Transform correctForward;
    // Start is called before the first frame update
    void Start()
    {
    }
}
